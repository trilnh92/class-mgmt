<script setup>
import { ref, onMounted } from "vue";

const props = defineProps(["modalTitle"]);

onMounted(() => {
//   if (props.modalTitle) {
//     console.log(props.modalTitle);
//   }
});
</script>

<template>

</template>